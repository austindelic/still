{
  "name": "cli",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "cargo run",
    "start": "cargo run --release",
    "build": "cargo build --release",
    "check": "cargo check",
    "preview": "cargo run --release"
  }
}
