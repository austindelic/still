[package]
name = "still-source-flatpak"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lib]
name = "still_source_flatpak"
path = "lib.rs"

[features]
default = []
portable = []

[dependencies]
serde = { workspace = true }
still-source-kit = { path = "../../source-kit" }
