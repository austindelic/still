[package]
name = "still-source-winget"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[lib]
name = "still_source_winget"
path = "lib.rs"

[features]
default = []
portable = []

[dependencies]
serde = { workspace = true }
still-source-kit = { path = "../../source-kit" }
